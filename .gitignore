# Scratch files written by rocq-mcp while stepping through a proof.
rocq_mcp_cache_*.v

# the phase-1 pruning table, 2.2 GB, rebuilt by ./rubik_par <d> <cap> build
phase1_cap*.tbl

# lia/nia caches
.lia.cache
.nia.cache

# .glob files copied off another machine, unpacked for ./unusedreq.py
code/Rubik/glob/

# generated by mkhsweep.sh and mkhall.sh, like the other emitted files
HAll.v
HSweepAll.v
HSweepCAll.v
HSweepE_[0-9][0-9].v
HSweep_[0-9][0-9].v
